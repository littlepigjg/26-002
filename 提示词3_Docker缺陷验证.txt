你现在是一个 Docker 验证专家。请对当前目录的项目完成以下操作，确认三件事：
1. Docker 镜像可在 x86/amd64 与 arm/arm64 两种架构下正常构建；
2. 服务在容器内能正常启动并响应（可运行）；
3. 注入的缺陷确实存在（单次触发即可，无需多轮），注意如果没有缺陷，需要修改代码，注入缺陷，如果有缺陷，不能修复缺陷，需要保持代码有缺陷。



缺陷信息（./验收.txt中获取）：
bug_id：【如 exam-slice-001】
bug_category：【如 slice】
缺陷描述：【如：自动组卷抽取数量未校验上限，超过候选池大小导致切片越界】
植入位置：【如：internal/service/paper_service.go.AutoGenerate】
预期表现：【如：count 大于候选池题数时 panic "slice bounds out of range"】
触发方式：【如：调用 POST /api/v1/papers/auto，count 设为大于候选池题数的值】

Docker 构建与运行步骤：

步骤 1：构建 Docker 镜像（只需构建 amd64 与 arm64 两种架构）

# 前置检查：若项目缺少 main.go 程序入口，需先生成 main.go 入口文件（并确保其可正常编译运行），再进行镜像构建
# 例如：若 main.go 不存在，则创建 main.go，包含启动 HTTP 服务所需的 main 函数与路由注册

# 构建 x86/amd64 镜像
./build_benzhi_docker.sh exam-system latest linux/amd64

# 构建 arm/arm64 镜像
./build_benzhi_docker.sh exam-system latest linux/arm64

要求：amd64 与 arm64 两种架构都必须构建成功。任一架构构建失败时，需要排查原因并修改
benzhi.Dockerfile 或 build_benzhi_docker.sh（如基础镜像、架构参数、依赖、编译命令等），
然后重新构建，直到两种架构都能成功构建为止。
（若单机无法直接构建另一架构，需安装 buildx 与 QEMU，或改用：
  docker buildx build -f benzhi.Dockerfile --platform linux/amd64,linux/arm64 -t exam-system:latest .）

步骤 2：运行容器

# 清理已有容器
docker rm -f test-verify 2>/dev/null || true

# 启动容器（x86 宿主机用 amd64 镜像，arm 宿主机用 arm64 镜像）
docker run -d --name test-verify \
  -p 8080:8080 \
  -v $(pwd):/app \
  exam-system:latest

步骤 3：容器内验证编译与运行

# 3.1 容器内编译与静态检查
docker exec test-verify /bin/bash -c "
cd /app
go build ./... && echo 'BUILD OK'
go vet ./... && echo 'VET OK'
"

# 3.2 验证服务可运行（等待健康检查通过）
for i in $(seq 1 30); do
  if curl -s http://localhost:8080/health >/dev/null 2>&1; then
    echo 'RUN OK (service healthy)'
    break
  fi
  sleep 1
done
# 若始终未输出 RUN OK，则判定服务无法运行。

步骤 4：容器内缺陷验证（单次触发）

根据缺陷类型执行对应命令，仅触发 1 次，判断缺陷是否真实存在（RED=缺陷存在 / GREEN=缺陷不存在）。


步骤 5：结果判定

测试完成后，输出以下总结：

========== 缺陷验证报告 ==========
bug_id: 【bug_id】
bug_category: 【bug_category】
Docker 构建 (x86/amd64): 【PASS/FAIL】
Docker 构建 (arm/arm64): 【PASS/FAIL】
容器内编译 (go build): 【PASS/FAIL】
容器内 go vet: 【PASS/FAIL】
服务可运行 (health): 【PASS/FAIL】
缺陷是否存在: 【是/否】
缺陷验证结果: 【RED（缺陷存在）/ GREEN（缺陷不存在）】
结论: 【缺陷已验证存在 / 缺陷未复现，需要调整植入方式】
==================================

清理：
测试完成后，清理容器：
docker rm -f test-verify 2>/dev/null || true

执行要求：
1. 必须在 Docker 容器内执行全部测试（不能在宿主机直接跑）
2. 缺陷验证只需触发 1 次，无需多轮
3. 必须同时验证 x86/amd64 与 arm/arm64 两种架构的镜像构建
4. 必须验证服务可运行（健康检查通过）
5. 记录缺陷是否真实存在（RED/GREEN）
6. 若缺陷未复现，说明注入方式需要调整，更新 验收.txt 中的触发步骤和 verify_cmds



